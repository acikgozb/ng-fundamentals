import { CreateSessionValidators } from './create-session-validators';

describe('CreateSessionValidators', () => {
  it('should create an instance', () => {
    expect(new CreateSessionValidators()).toBeTruthy();
  });
});
